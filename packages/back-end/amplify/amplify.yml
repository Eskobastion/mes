version: 1
applications:
  - appRoot: packages/back-end/amplify
    backend:
      phases:
        build:
          commands:
            - npm ci --cache .npm --prefer-offline
            - npx ampx pipeline-deploy --branch $AWS_BRANCH --app-id $AWS_APP_ID
   
 #   frontend:
 #     phases:
 #       build:
 #         commands:
 #           - npx nx build amplify
 #     artifacts:
 #       baseDirectory: dist
 #       files:
 #         - '**/*'
 #     cache:
 #       paths:
 #         - .npm/**/*
 #     buildPath: /
    
